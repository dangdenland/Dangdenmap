// ==========================================
// 🌍 地圖設定檔 (每次換圖只要改這裡)
// ==========================================

const mapSettings = {
    // 1. 圖片檔名
    imageName: "map_bg.png",

    // 2. 圖片對應的遊戲座標範圍 (從繪圖軟體或遊戲匯出時取得)
    // 左邊界 (X Min)
    minX: -2560,
    
    // 右邊界 (X Max)
    maxX: 9359,
    
    // 上邊界 (Y Min, 遊戲中較小的 Y 值或負值通常在上方)
    minY: -9728,
    
    // 下邊界 (Y Max, 遊戲中較大的 Y 值通常在下方)
    maxY: 1023
};

// ==========================================
// 📐 換算公式 (給您參考，不用改程式碼)
// ==========================================
// 如果您只知道左上角座標，想算出右下角：
// 右邊界 = 左邊界 + (圖片寬度像素 * 縮放倍率)
// 下邊界 = 上邊界 + (圖片高度像素 * 縮放倍率)
// (例如 1:4 的地圖，縮放倍率就是 4)